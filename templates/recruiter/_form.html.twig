{{ form_start(form) }}

        <div class="container ms-5 w-50 ">     
        
        <div class="form-group text-left mt-3">
            {{ form_label(form.company,'Societé' ,{'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.company, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.company) }}
        </div> 

        <div class="form-group text-left mt-3">
            {{ form_label(form.city,'Ville' ,{'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.city, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.city) }}
        </div> 

        <button class="btn btn-primary mt-4">{{ button_label|default('Enregistrer') }}</button>
    </div> 



{{ form_end(form) }}
