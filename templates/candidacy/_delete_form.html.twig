<form method="post" action="{{ path('candidacy_delete', {'id': candidacy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidacy.id) }}">
    <button class="btn"><img class="picto" title= "supprime" src="{{ asset('assets/images/bin.png') }}" alt="bin icon"></a></button>
</form>
