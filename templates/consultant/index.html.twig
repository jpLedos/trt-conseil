{% extends 'base.html.twig' %}

{% block title %}Consultant index{% endblock %}

{% block body %}
    <h1>Consultant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lastname</th>
                <th>Firstname</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for consultant in consultants %}
            <tr>
                <td>{{ consultant.id }}</td>
                <td>{{ consultant.lastname }}</td>
                <td>{{ consultant.firstname }}</td>
                <td>
                    <a href="{{ path('consultant_show', {'id': consultant.id}) }}">show</a>
                    <a href="{{ path('consultant_edit', {'id': consultant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('consultant_new') }}">Create new</a>
{% endblock %}
