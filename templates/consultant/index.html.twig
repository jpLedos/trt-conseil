{% extends 'base.html.twig' %}

{% block title %}Consultant index{% endblock %}

{% block body %}
    <div class= "container w-75">
        <h1>Consultant index</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>email</th>
                    <th>Lastname</th>
                    <th>Firstname</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for consultant in consultants %}
                <tr>
                    <td>{{ consultant.id }}</td>
                    <td>{{ consultant.user.email }}</td>
                    <td>{{ consultant.lastname }}</td>
                    <td>{{ consultant.firstname }}</td>
                    <td>
                        <a href="{{ path('consultant_edit', {'id': consultant.id}) }}">
                           <img class="picto" title= "edit" src="{{ asset('assets/images/edition.png') }}" alt="edit icon"></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('consultant_new') }}">Créer un nouveau consultant</a>
    </div>
{% endblock %}
